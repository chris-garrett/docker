name: "Devops: Build"

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - Dockerfile.devops
      - Dockerfile.devops.sbom

  pull_request:
    branches:
      - main
    paths:
      - Dockerfile.devops
      - Dockerfile.devops.sbom

concurrency:
  group: devops-build-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  update:
    name: Build image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Build Image
        uses: ./.github/actions/build-image
        with:
          service_name: devops
          github_token: ${{ secrets.GITHUB_TOKEN }}
